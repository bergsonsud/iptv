<%= form_with(model: url_link, local: true) do |form| %>
  <% if url_link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(url_link.errors.count, "error") %> prohibited this url_link from being saved:</h2>

      <ul>
      <% url_link.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
